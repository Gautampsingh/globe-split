# GlobeSplit

Minimal scaffold created. You can fill component/service contents.

## Quick Start (PowerShell)

```
# Install deps
npm install

# Placeholder scripts; replace with Angular dev server or Vite
npm run start
npm run build
```

Replace package.json scripts with your chosen builder (Angular CLI or Vite).
